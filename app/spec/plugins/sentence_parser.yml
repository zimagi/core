plugin:
  sentence_parser:
    base: base
    interface:
      split:
        params:
          text: str
        returns: list

    option:
      max_sentence_length:
        type: int
        help: 'Maximum length of parsed sentence'
        default: 3500

    providers:
      spacy:
        option:
          model:
            type: str
            help: 'Sentence parser model name'
            default: en_core_web_lg
          validate:
            type: bool
            help: 'Whether or not to check sentences for nouns, verbs, and propositions'
            default: true
