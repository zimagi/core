plugin:
  file_parser:
    base: base
    interface:
      parse:
        params:
          file_path: str
        returns: str

    providers:
      txt:
      md:
      binary:
      docx:
        base: binary
      pptx:
        base: binary
      xlsx:
        base: binary
      csv:
      pdf:
        base: binary
