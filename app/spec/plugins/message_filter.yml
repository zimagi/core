plugin:
  message_filter:
    base: base
    interface:
      filter:
        params:
          message: dict
          value: int|float|str|bool
        returns: dict|null

    providers:
      mentions_me:
      field_exists:
